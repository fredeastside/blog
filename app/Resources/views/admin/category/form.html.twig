{{ form_start(form) }}
{{ form_row(form.name) }}
{% if form.vars.value and form.vars.value.picture %}
    <img style="height: 200px;" src="{{ asset('upload/' ~ form.vars.value.picture.filename) }}" /><br/>
{% endif %}
{{ form_row(form.picture) }}
{{ form_end(form) }}